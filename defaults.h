#ifndef DSSI_CONV_DEFAULTS_H
#define DSSI_CONV_DEFAULTS_H

/* 
	make it rather huge, so new users don't waste their session
	with excessive cpu load  :) 
*/
#define DEFAULT_PARTITION_SIZE 16384

#endif
